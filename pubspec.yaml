name: llvm
description: Dart bindings to the LLVM C-API
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  ffi: ^0.1.3
  meta: ^1.1.8

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
  build_runner: ^1.10.0
  build_ffi_generator:
    hosted:
      name: build_ffi_generator
      url: https://kneipe.simonbinder.eu/build_ffi_generator/master